# ===================================================================
# General .NET & Visual Studio
# https://github.com/github/gitignore/blob/main/VisualStudio.gitignore
# ===================================================================

## User-specific files
*.rsuser
*.suo
*.user
*.userosscache
*.sln.docstates

## Build results
[Dd]ebug/
[Dd]ebugPublic/
[Rr]elease/
[Rr]eleases/
x64/
x86/
[Ww][Ii][Nn]32/
[Aa][Rr][Mm]/
[Aa][Rr][Mm]64/
bld/
[Bb]in/
[Oo]bj/
[Ll]og/
[Ll]ogs/

# Application logs
logs/
*.log

## Visual Studio / Rider specific
.vs/
.idea/
*.sln.DotSettings.user
*.dbmdl
*.[Pp]ublish.xml
*.azurePubxml
*.publishproj

# = a '.DotSettings' file is implicitly generated by ReSharper and Rider,
# even if there are no solution-specific settings.
# It can be safely ignored.
*.DotSettings

## NuGet Packages
# NuGet packages are managed via the .csproj file.
# The 'packages' folder is for old-style package management.
packages/
*.nupkg
# except build/, which is used as an MSBuild target.
!/packages/build/

## Secrets and environment-specific settings
# User secrets file for development (NEVER commit this!)
secrets.json
appsettings.*.json

# Keep appsettings.json for default settings, but ignore development overrides
!appsettings.json
!appsettings.Production.json
# You might choose to commit Production settings, but often these are managed by CI/CD.
# If you manage production settings outside of git, uncomment the line above.

# Local launch settings (e.g., debug profiles, environment variables for local dev)
# It's debatable, but generally good to ignore to avoid conflicts with teammates' settings.
Properties/launchSettings.json

## Other
*.bak
*.tmp
*.log
*.DS_Store
Thumbs.db
*.tsserver.tsbuildinfo

# Test Results
TestResults/
*.trx

# Code Coverage
*.coverlet.json
*.cobertura.xml

# Entity Framework
# These are migration files that are auto-generated.
# Usually, you DO want to commit migrations, so these are commented out.
# If your team has a different workflow (e.g., generating from a master DB), you might ignore them.
# **/Migrations/*.cs


.qoder